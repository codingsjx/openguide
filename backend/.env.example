# Copy to `.env` and fill in local-only secrets. Never commit this file.
# GITHUB_TOKEN: optional; leave empty to run unauthenticated (lower rate limits).
GITHUB_TOKEN=
# OpenAI-compatible relay credentials (local only).
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-5.5
